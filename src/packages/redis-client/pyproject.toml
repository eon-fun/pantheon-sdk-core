[project]
name = "redis-client"
version = "0.1.5"
description = "Package that create Redis client"
authors = [
    {name = "k1llzzy",email = "gekachan04@gmail.com"}
]
requires-python = ">=3.10,<4"
dependencies = [
    "redis>=4.5.0",
]
readme = "README.md"

[tool.poetry]
packages = [{include = "*", from="src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
